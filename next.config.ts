const nextConfig = {
  output: "export",

  // basePath: "/rajat_king",
  // assetPrefix: "/rajat_king/",

  images: {
    unoptimized: true,
  },
  devIndicators: false,
};

module.exports = nextConfig;
